version: 2
updates:
  # Python deps (uv.lock is updated via Dependabot's native uv ecosystem).
  - package-ecosystem: "uv"
    directory: "/backend"
    schedule:
      interval: "weekly"

  # GitHub Actions used in workflows.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  # Base image in the backend Dockerfile.
  - package-ecosystem: "docker"
    directory: "/backend"
    schedule:
      interval: "weekly"
